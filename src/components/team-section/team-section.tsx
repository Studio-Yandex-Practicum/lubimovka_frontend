import { FC } from 'react';
import cn from 'classnames';

interface ITeamSectionProps {
  content: {
    id: number
    name: string
    link: string
    response: string
  }
}

const TeamSection: FC<ITeamSectionProps> = ({ content }) => {


  return (
    <>
    </>
  );
};

export default TeamSection;
