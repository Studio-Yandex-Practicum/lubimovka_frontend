import { ScheduleLayout as Component } from './schedule-layout';
import { ScheduleLayoutSlot } from './schedule-layout-slot';

export const ScheduleLayout = Object.assign(Component, {
  Slot: ScheduleLayoutSlot,
});
