import { PressReleaseLayout as Component } from './press-release-layout';
import { PressReleaseLayoutSlot } from './press-release-layout-slot';

export const PressReleaseLayout = Object.assign(Component, {
  Slot: PressReleaseLayoutSlot,
});
