import { SearchLayout as Component } from './search-layout';
import { SearchLayoutSlot } from './search-layout-slot';

export const SearchLayout = Object.assign(Component, {
  Slot: SearchLayoutSlot,
});
