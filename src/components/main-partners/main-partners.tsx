import { FC } from 'react';

import styles from './main-partners.module.css';

export const MainPartners: FC = () => {
  return (
    <section className={styles.section}>
      <h2 className={styles.title}>Компонент Partners</h2>
    </section>
  );
};
