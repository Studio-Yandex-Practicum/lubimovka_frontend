import { CheckboxGroup as Component } from './checkbox-group';
import { CheckboxGroupItem } from './checkbox-group-item';

export const CheckboxGroup = Object.assign(Component, {
  Item: CheckboxGroupItem,
});
